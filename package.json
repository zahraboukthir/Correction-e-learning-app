{
  "name": "success-way",
  "version": "1.0.0",
  "description": "concurrence backend & frontend application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm start \" \"cd frontend && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohamed Wanna",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
